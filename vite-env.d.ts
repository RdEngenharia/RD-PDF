// FIX: The reference to "vite/client" was removed to resolve a "Cannot find type definition file" error.
// This is likely caused by a tsconfig.json misconfiguration that cannot be modified here.
// The application does not use Vite-specific client APIs, so this change is safe.
