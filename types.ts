
import type { PDFDocument, PDFImage, Rotation } from 'pdf-lib';

// As bibliotecas agora são importadas como módulos, então as declarações globais não são mais necessárias.
// Mantemos os exports para que outros arquivos possam continuar importando os tipos.

export type { PDFDocument, PDFImage, Rotation };
